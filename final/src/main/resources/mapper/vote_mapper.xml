<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.team.apt.vote.mapper.VoteMapper">
	
	
	<select id="voteList" resultType="vote" parameterType="vote">
		select *
		from boardvote
		order by seq
	</select>
	
	<select id="selectOne" resultType="vote" parameterType="vote">
		select *
		from boardvote
		where seq=#{seq}
	</select>
	
	<insert id="voteInsert" parameterType="vote">
		insert into boardvote(
								seq,
								title,
								startdate
								enddate )
		values(
				seq_vote.nextval,
				#{title},
				#{startdate}
				#{enddate} )
	</insert>
	
	<update id="voteUpdate" parameterType="vote">
		update board
		set title=#{title},
			content=#{content},
			startdate=#{sysdate},
			enddate=#{enddate}
		where seq = #{seq}
	</update>

	<delete id="voteDelete" parameterType="vote">
		delete from board
		where seq = #{seq}
	</delete>
</mapper>
