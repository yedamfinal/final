<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.team.apt.person.mapper.PersonMapper">
	<select id="loginCheck" resultType="ResidentVo" parameterType="ResidentVo">
		select *
		from(
		select 'r' type, id, name, password,owner from resident
		union all
		select 'm' type, id, name, password,king from manager
		)
		where id=#{id}
		and password=#{password}
	</select>
	
	<select id="loginResident" resultType="ResidentVo" parameterType="ResidentVo">
		select a.*, 'r' type 
		from resident a
		where id=#{id}
	</select>
	
	<select id="loginManager" resultType="ManagerVo" parameterType="ResidentVo">
		select * 
		from manager
		where id=#{id}
	</select>
	

</mapper>
