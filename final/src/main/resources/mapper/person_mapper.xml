<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="co.team.apt.person.mapper.PersonMapper">
	<select id="loginCheck" resultType="resident" parameterType="resident">
		select *
		from(
		select id,password,owner from resident
		union all
		select id, password,king from manager
		)
		where id=#{id}
		and password=#{password}
	</select>
	
	<select id="loginResident" resultType="resident" parameterType="resident">
		select * 
		from resident
		where id=#{id}
		and password=#{password}
	</select>
	
	<select id="loginManager" resultType="manager" parameterType="resident">
		select * 
		from manager
		where id=#{id}
		and password=#{password}
	</select>
	

</mapper>
